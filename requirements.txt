# Core Flask dependencies
flask>=3.0,<4
gunicorn>=21.2

# Data processing and ML
numpy>=2.1
pandas>=2.2.3
scikit-learn>=1.3

# Visualization
plotly>=5.22

# Production WSGI server (for Linux/macOS)
# Note: Hostinger typically uses mod_wsgi or similar, but gunicorn is included as backup